OH MON DIEU C'EST TOUT DOUX

- leaks parsing
-	ls << a << b segfault
- echo "blblb\n" | cat -e
- echo -n + fleche gauche (probleme avec le prompt)
- check des TODO dans le code pour supprimer les variables inutiles
- "ls ||" et "ls &&" qui buguent

{
	commande -> {
		nom du programme
		arguments
	} (av)

	redirections -> {
		> >> 2> 2>>
		aggregation fd
		}
}

ls 2> truc 2>&1 peni

ls > a > b

{
	av;
	file_out; 1>&1
	file_in;
	file_err; 2>&1
	fd_out; 2 - b
	fd_in; 0
	fd_err; 2 - b
}

pipe -> in : 3 out : 4

on ecrit dans le in
on ferme le in
on lit dans le out
